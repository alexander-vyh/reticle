#!/usr/bin/env sh
# --- BEGIN BEADS INTEGRATION v0.59.0 ---
# This section is managed by beads. Do not remove these markers.
if command -v bd >/dev/null 2>&1; then
  export BD_GIT_HOOK=1
  bd hooks run post-checkout "$@"
  _bd_exit=$?; if [ $_bd_exit -ne 0 ]; then exit $_bd_exit; fi
fi
# --- END BEADS INTEGRATION v0.59.0 ---

# --- BEGIN REPO IDENTITY ---
# Override git author for this repo (public repo uses GitHub identity, not work email)
git config --local user.name "alexander-vyh"
git config --local user.email "alexander-vyh@users.noreply.github.com"
# --- END REPO IDENTITY ---
